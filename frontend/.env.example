# Copy to frontend/.env for local development. Never commit the real .env.
# Base URL of the FastAPI backend, consumed via runtimeConfig.public.apiBase.
NUXT_PUBLIC_API_BASE=http://localhost:8000/api/v1
